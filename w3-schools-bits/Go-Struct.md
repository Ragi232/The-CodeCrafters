## Go Struct

A Go struct is a way to group different pieces of data together into one unit in the Go programming language. think of it like creating your own custom data type.

## e.g 

type Person struct {
  name string
  age int
  job string
  salary int
}