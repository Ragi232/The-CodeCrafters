## Go Constants 

Constants in Go are like variables that never change. Once you give them a value, it cannot be modified anywhere in your program.Constants in Go are fixed values that cannot change during program execution, used to make code safer, clearer, and more reliable.