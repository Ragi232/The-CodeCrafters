## Go For Loops

In Go there is only one loop keyword (for) but it can be used in multiple ways to achieve different looping behaviors.The for loop is the only loop available in Go.                                     

## e.g 
package main
import ("fmt")

func main() {
  for i:=0; i < 5; i++ {
    fmt.Println(i)
  }
}